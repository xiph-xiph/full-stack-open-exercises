{
  "name": "blog_list",
  "version": "1.0.0",
  "type": "module",
  "main": "./src/index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node .",
    "dev": "cross-env NODE_ENV=development node --watch --inspect .",
    "test": "cross-env NODE_ENV=test node --test",
    "test-only": "cross-env NODE_ENV=test node --test --test-only",
    "lint": "eslint ."
  },
  "author": "",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "@stylistic/eslint-plugin": "^5.2.2",
    "eslint": "^9.32.0",
    "globals": "^16.3.0",
    "supertest": "^7.1.4"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cross-env": "^10.0.0",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "mongoose": "^8.17.0"
  }
}
